// Customer pages now have their own headers and navigation
// This layout is kept for compatibility but is minimal
export default function CustomerLayout({ children }) {
  return <div>{children}</div>;
}
